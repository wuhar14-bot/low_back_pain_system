# ==================================================
# Production Environment Configuration
# 生产环境配置
# ==================================================

# .NET Backend API URL (Render deployment)
VITE_API_URL=https://low-back-pain-backend.onrender.com

# 注意: Python服务不直接暴露给前端
# Python services are accessed through .NET API

# 调试模式
VITE_DEBUG=false

# 临时禁用认证 (Temporarily disable authentication for testing)
VITE_DISABLE_AUTH=true

# API超时设置 (毫秒)
VITE_API_TIMEOUT=30000
VITE_OCR_TIMEOUT=60000
VITE_POSE_TIMEOUT=60000
